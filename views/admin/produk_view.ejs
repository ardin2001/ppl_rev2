<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>Document</title>
</head>
<body>
    <div class="container">
        <a href="/admin/produk/createproduk" class="btn btn-success">Tambah Produk</a>
        <table class="table">
            <thead>
              <tr>
                <th scope="col">id</th>
                <th scope="col">Nama Barang</th>
                <th scope="col">Deskripsi</th>
                <th scope="col">Harga</th>
                <th scope="col">Gambar</th>
                <th>Action</th>
              </tr>
            </thead>
            <tbody>
                <% Produk.forEach(e => { %>
                    <tr>
                        <td><%= e.id_barang %> </td>
                        <th><%= e.nama_barang %> </th>
                        <td><%= e.deskripsi %> </td>
                        <td><%= e.harga %> </td>
                        <td><img src="../img/<%= e.gambar %>" width="60px" height="60px"></td>
                        <td>
                            <a href="/admin/produk/edit/<%= e.id_barang %> " class="btn btn-warning">Edit</a>
                            <button onclick= <%= "del("+e.id_barang+")" %>  type="button" class="btn btn-danger" >Delete</button>
                        </td>
                    </tr>
                <% }) %>
            </tbody>
          </table>
    </div>
    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
    <script>
        const del = (id) =>{
            alert('Are you delete id : ' +id)
            axios.delete('/admin/api/produk/'+id).then(result =>{
                alert('info status : '+ result.status)
                location.reload()
            }).catch(err =>{
                alert('error : '+err)
            })
        }
    </script>
    
</body>
</html>